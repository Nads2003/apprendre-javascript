/*Un fichier peut avoir un seul export par défaut, généralement utilisé pour la « chose principale »
du module (une classe, une fonction).*/

export default class Utilisateur {
 constructor(nom) {
 this.nom = nom;
 }
}
